apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-config
  namespace: flask-mongodb
data:
  init-mongo.js: |
    db.createUser({
      user: "flaskuser",
      pwd: "flaskpass123",
      roles: [{ role: "readWrite", db: "flask_db" }]
    })
